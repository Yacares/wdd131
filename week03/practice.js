let firstName = 'Antonia';
let lastName = 'Francesca';

function completeName (firstName,lastName){

    let name = firstName + " " + lastName;
    return name;
}

console.log(completeName(firstName,lastName));